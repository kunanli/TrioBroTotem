# Godot 4
.godot/
/android/

# Godot 4.4+ 會替每個腳本產生 .uid。官方建議進版控，但這個專案的場景一律用
# res:// 路徑引用（沒有任何一個 uid://），所以 uid 沒有作用；而不同機器各自
# 產生的 uid 內容不同，進了版控只會在每次 pull 時打架。等到哪天真的用上
# uid:// 引用再改回來。
*.uid
export_presets.cfg
*.translation

# 匯出產物
/build/
/export/
*.pck
*.exe
*.x86_64
*.app
*.dmg
*.zip

# 模型與貼圖的二進位不進版控（見 assets/source/README.md）
#
# 理由：Meshy 的原始匯出隨時可以重新下載，而且每次重生成都是新的一份。
# 它們又大（單一角色連貼圖 80 MB）又不是最終資產。版控只放正規化後、
# 真正進遊戲的檔案——那些放在 trio-project/ 底下，每隻只有幾 MB。
#
# 不限定 assets/source/：角色資料夾放在 assets/ 哪一層都會被擋到。
assets/**/*.fbx
assets/**/*.glb
assets/**/*.gltf
assets/**/*.obj
assets/**/*.blend
assets/**/*.png
assets/**/*.jpg
assets/**/*.jpeg
assets/**/*.tga
assets/**/*.exr

# 但參考資料要留著（關卡草圖、流程圖）
!assets/reference/**

# 美術暫存
*.blend1
*.blend2
*.psd~

# 作業系統
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc

# 編輯器
.vscode/
.idea/
*.swp
