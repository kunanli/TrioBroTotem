# Godot 4
.godot/
/android/

# export_presets.cfg 刻意**進**版控。
#
# 一般的 Godot .gitignore 會擋它，理由是裡面可能有簽章金鑰與本機路徑。
# 這個專案的預設檔沒有那些（codesign 關著、路徑是相對的 ../build/），
# 而它正是朋友要拿到建置檔的唯一依據——擋掉的話 --export-release 會說
# 找不到 preset，而那個錯誤看起來完全不像「檔案沒進版控」。
# 之後真的要簽章時，金鑰另外用環境變數帶，不要寫進這個檔。
*.translation

# 匯出產物
/build/
/export/
*.pck
*.exe
*.x86_64
*.app
*.dmg
*.zip

# 模型與貼圖的二進位不進版控（見 assets/source/README.md）
#
# 理由：Meshy 的原始匯出隨時可以重新下載，而且每次重生成都是新的一份。
# 它們又大（單一角色連貼圖 80 MB）又不是最終資產。版控只放正規化後、
# 真正進遊戲的檔案——那些放在 trio-project/ 底下，每隻只有幾 MB。
#
# 不限定 assets/source/：角色資料夾放在 assets/ 哪一層都會被擋到。
assets/**/*.fbx
assets/**/*.glb
assets/**/*.gltf
assets/**/*.obj
assets/**/*.blend
assets/**/*.png
assets/**/*.jpg
assets/**/*.jpeg
assets/**/*.tga
assets/**/*.exr

# 但參考資料要留著（關卡草圖、流程圖）
!assets/reference/**

# 美術暫存
*.blend1
*.blend2
*.psd~

# 作業系統
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc

# 編輯器
.vscode/
.idea/
*.swp
